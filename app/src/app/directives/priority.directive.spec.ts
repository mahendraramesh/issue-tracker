import { PriorityDirective } from './priority.directive';

describe('PriorityDirective', () => {
  it('should create an instance', () => {
    const directive = new PriorityDirective();
    expect(directive).toBeTruthy();
  });
});
